# Capture the real Django/S3 error in logs if collectstatic fails.
container_commands:
  02_collectstatic:
    command: "source /var/app/venv/*/bin/activate && python3 manage.py collectstatic --noinput -v2"
    leader_only: true
